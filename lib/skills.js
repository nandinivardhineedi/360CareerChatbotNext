  export function getSkillInsights(domain, level) {
    if (domain === "Data Science") {
      return [
        "- Python is essential; try small projects.",
        "- NumPy, Pandas, and Matplotlib are core tools.",
        "- Learn basic statistics and simple visualizations."
      ];
    }
    // Add tips for other domains...
    return ["Explore basic skill-building resources online."];
  }
